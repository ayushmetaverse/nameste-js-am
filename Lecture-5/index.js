// Shortest Program in JS

//empty file is the shortest program in JS


//In this file window is the global object which executes While GEC executes in the call stack with global object.

// Global object:- Window
  console.log(this===window);